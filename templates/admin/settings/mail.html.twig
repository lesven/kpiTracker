{% extends 'base.html.twig' %}

{% block title %}E-Mail Einstellungen - KPI Tracker{% endblock %}

{% block page_title %}E-Mail Einstellungen{% endblock %}

{% block page_actions %}
    <a href="{{ path('app_admin_dashboard') }}" class="btn btn-outline-secondary btn-sm">
        <i class="bi bi-arrow-left"></i> Zurück
    </a>
{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0"><i class="bi bi-envelope"></i> Mailversand konfigurieren</h5>
            </div>
            <div class="card-body">
                {{ form_start(form) }}
                {{ form_row(form.host) }}
                <div class="row">
                    <div class="col-md-6">{{ form_row(form.port) }}</div>
                    <div class="col-md-6 d-flex align-items-center">{{ form_row(form.ignoreCertificate) }}</div>
                </div>
                {{ form_row(form.username) }}
                {{ form_row(form.password) }}
                <div class="d-flex justify-content-between mt-3">
                    <a href="{{ path('app_admin_dashboard') }}" class="btn btn-outline-secondary">
                        <i class="bi bi-x-circle"></i> Abbrechen
                    </a>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-check-circle"></i> Speichern
                    </button>
                </div>
                {{ form_end(form) }}
            </div>
        </div>
    </div>
</div>
{% endblock %}
