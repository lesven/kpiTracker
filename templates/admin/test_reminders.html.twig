{% extends 'base.html.twig' %}

{% block title %}Test-Erinnerungen{% endblock %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">
                <i class="fas fa-envelope"></i> Test-Erinnerungen
            </h1>

            <!-- Breadcrumb -->
            <nav aria-label="breadcrumb" class="mb-4">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="{{ path('app_admin_dashboard') }}">Admin</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Test-Erinnerungen</li>
                </ol>
            </nav>

            <!-- Flash Messages -->
            {% for type, messages in app.flashes %}
                {% for message in messages %}
                    <div class="alert alert-{{ type == 'error' ? 'danger' : type }} alert-dismissible fade show">
                        {{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}
            {% endfor %}

            <div class="row">
                <!-- Test-E-Mail senden -->
                <div class="col-lg-6">
                    <div class="card mb-4">
                        <div class="card-header">
                            <h5 class="card-title mb-0">
                                <i class="fas fa-paper-plane"></i> Test-E-Mail senden
                            </h5>
                        </div>
                        <div class="card-body">
                            <p class="text-muted">
                                Senden Sie eine Test-E-Mail, um die E-Mail-Konfiguration zu überprüfen.
                            </p>
                            
                            <form method="post" action="{{ path('app_admin_test_reminders') }}">
                                <div class="mb-3">
                                    <label for="test_email" class="form-label">E-Mail-Adresse</label>
                                    <input type="email" 
                                           class="form-control" 
                                           id="test_email" 
                                           name="test_email" 
                                           value="{{ test_email }}"
                                           placeholder="test@example.com"
                                           required>
                                    <div class="form-text">
                                        Die Test-E-Mail wird an diese Adresse gesendet.
                                    </div>
                                </div>
                                
                                <button type="submit" class="btn btn-primary">
                                    <i class="fas fa-paper-plane"></i> Test-E-Mail senden
                                </button>
                            </form>

                            {% if result %}
                                <div class="mt-3">
                                    {% if result.success %}
                                        <div class="alert alert-success">
                                            <i class="fas fa-check-circle"></i>
                                            Test-E-Mail erfolgreich an <strong>{{ result.email }}</strong> gesendet!
                                        </div>
                                    {% else %}
                                        <div class="alert alert-danger">
                                            <i class="fas fa-exclamation-triangle"></i>
                                            Fehler beim Senden an <strong>{{ result.email }}</strong>
                                            {% if result.error %}
                                                <br><small>{{ result.error }}</small>
                                            {% endif %}
                                        </div>
                                    {% endif %}
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>

                <!-- Alle Erinnerungen senden -->
                <div class="col-lg-6">
                    <div class="card mb-4">
                        <div class="card-header">
                            <h5 class="card-title mb-0">
                                <i class="fas fa-bell"></i> Alle Erinnerungen senden
                            </h5>
                        </div>
                        <div class="card-body">
                            <p class="text-muted">
                                Sendet alle fälligen Erinnerungen manuell. Normalerweise werden diese automatisch per Cron-Job versendet.
                            </p>
                            
                            <form method="post" action="{{ path('app_admin_send_all_reminders') }}" 
                                  onsubmit="return confirm('Möchten Sie wirklich alle fälligen Erinnerungen senden?');">
                                <input type="hidden" name="_token" value="{{ csrf_token('send_reminders') }}">
                                
                                <button type="submit" class="btn btn-warning">
                                    <i class="fas fa-bell"></i> Alle Erinnerungen senden
                                </button>
                            </form>
                            
                            <div class="mt-3">
                                <small class="text-muted">
                                    <i class="fas fa-info-circle"></i>
                                    Dies umfasst:
                                    <ul class="mt-2">
                                        <li>Vorab-Erinnerungen (3 Tage vor Fälligkeit)</li>
                                        <li>Erinnerungen für heute fällige KPIs</li>
                                        <li>Überfällige Erinnerungen (7 und 14 Tage)</li>
                                        <li>Eskalationen an Admins (21+ Tage)</li>
                                    </ul>
                                </small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- MailHog Info -->
            <div class="row">
                <div class="col-12">
                    <div class="card border-info">
                        <div class="card-header bg-info text-white">
                            <h5 class="card-title mb-0">
                                <i class="fas fa-info-circle"></i> MailHog - E-Mail-Testing
                            </h5>
                        </div>
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col-md-8">
                                    <p class="mb-2">
                                        <strong>MailHog</strong> fängt alle ausgehenden E-Mails ab und zeigt sie in einer Web-Oberfläche an.
                                        Perfekt für lokales Testing ohne echten E-Mail-Versand.
                                    </p>
                                    <ul class="mb-0">
                                        <li>Alle Test-E-Mails werden hier angezeigt</li>
                                        <li>Keine echten E-Mails werden versendet</li>
                                        <li>Ideal für Entwicklung und Testing</li>
                                    </ul>
                                </div>
                                <div class="col-md-4 text-end">
                                    <a href="http://localhost:8025" 
                                       target="_blank" 
                                       class="btn btn-info btn-lg">
                                        <i class="fas fa-external-link-alt"></i>
                                        MailHog öffnen
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Zurück Button -->
            <div class="row mt-4">
                <div class="col-12">
                    <a href="{{ path('app_admin_dashboard') }}" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Zurück zum Admin-Dashboard
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
